# ── 核心（一定要裝）─────────────────────────────
pandas>=2.0
numpy>=1.24
pyyaml>=6.0
openpyxl>=3.1          # 讀寫 .xlsx（POS 與回饋表）
xlrd>=2.0              # 讀舊版 .xls（進銷存報表多為此格式）
pyarrow>=14.0          # parquet 中繼檔

# ── 影像特徵（Fashion-CLIP）───────────────────
# CPU 也跑得動；3000 張系統圖約 10–20 分鐘。
# 有 NVIDIA 顯卡時改裝 CUDA 版 torch 會快 10 倍以上：
#   https://pytorch.org/get-started/locally/
torch>=2.0
torchvision>=0.15
transformers>=4.35
Pillow>=10.0

# ── 統計與模型 ────────────────────────────────
scikit-learn>=1.3      # 特徵重要度、版群分析
scipy>=1.11            # 精確的卡方 p 值

# ── 線稿與影像處理 ────────────────────────────
opencv-python-headless>=4.8

# ── 網頁後台 ──────────────────────────────────
fastapi>=0.109
uvicorn[standard]>=0.27
python-multipart>=0.0.9   # 檔案上傳

# ── 選用（沒裝也能跑，功能會降級）──────────────
# pdfplumber>=0.10       # 解析文字型 PDF 裁縫指示書
# pytesseract>=0.3.10    # 掃描件 OCR（另需安裝 Tesseract 本體 + 中文語言包）
# faiss-cpu>=1.7         # 商品數 > 5 萬時的快速檢索
# pypotrace>=0.3         # 更高品質的線稿向量化（需先裝 libpotrace）
