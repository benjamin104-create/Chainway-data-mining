# Chainway 以圖查貨號 —— 現況交接

給在**本機**接手的 Claude Code。使用者是 Kinloch Anderson 的設計總監，
不是工程師；他要的是「拍一張照片，回答貨號、顏色、尺寸、庫存」。

## 現在卡在哪（唯一重要的事）

`cli fingerprint` 的就地自我驗證跑出來是：

    40 題　Top-1 15.0%　Top-5 20.0%

題目是「把系統圖退化成手機隨手拍，再查回原圖」—— 這是最容易的題型，
15% 等於沒在運作。

**Top-5 只比 Top-1 高 5 個百分點是最大的線索。** 正常的檢索系統
Top-5 會遠高於 Top-1（例如 85% / 97%）。兩個數字貼這麼近，代表答錯的
時候正解根本不在前面，不是差一點 —— 是結構性的問題，不是調參數能救的。

### 第一件該做的事：打開系統圖來看

先前所有診斷都是隔著檔案猜的，因為雲端那邊讀不到圖。本機可以直接看。

    C:\Users\USER\Desktop\商品設計Raw Data\系統圖

要確認的：畫面裡是什麼（平放去背？模特兒？型錄頁？一張圖多件商品？）、
背景是不是白的、商品佔畫面多少、有沒有文字浮水印。

`chainway/search/selfeval.py` 的 `simulate()` 是拿「棚拍平放圖」的假設
去出題的；如果實際不是那樣，出的題目本身就是錯的，分數自然沒有意義。

### 已經排除的

* **不是雜訊門檻。** 關鍵點門檻已改成「比第二名高 4 倍」（純亂數雜訊圖
  在真實庫上能拿到 41～93 分，絕對門檻完全失效）。見 `find.py` 的
  `KP_DOMINANCE`。
* **不是每款只存一個顏色。** 那個 bug 是真的、也修好了（fp1→fp2，一列
  = 一張參考圖），但實際資料是 4,055 張圖 / 3,320 款 ≈ 一款 1.2 張，
  所以它不是主因。
* **不是指紋讀寫壞掉。** `cli bench --roundtrip` 走完整條匯出路，通過。

## 使用者立過的規矩

* **資料夾唯讀。** 絕對不要修改、搬動、刪除 `商品設計Raw Data` 底下任何
  東西，也不要碰 `.agents`、`AGENTS.md`、`INDEX.md` 或既有的 Codex 設定。
  所有產出寫進專案自己的 `data/`。
* **不要變造資料。** 查不到就說查不到，不要編一個看起來合理的貨號。
* **不要叫他修圖、裁圖、標答案。** 他說過「不是叫使用者一直幫你修圖編圖
  的」。要標註資料就自己想辦法（每張系統圖的檔名就是答案，不需要人工）。
* **比對不准用 AI／模型／API。** 只能用 numpy + Pillow + OpenCV 的算術。
  這樣才能做成公司內部的靜態查詢工具。
* `開始.bat` 必須是純 ASCII（CRLF 由 `.gitattributes` 處理）。
* 分支：`claude/fashion-sales-design-platform-vabg76`。

## 系統長什麼樣

三關，每一關都是**證據**，不是條件：

    特徵（品名 IDF）→ 顏色（多尺度格子簽名）→ 關鍵點（ORB + RANSAC）

* `chainway/vision/grid.py` —— 顏色簽名（2×2/3×3/4×4 格中位 LAB + 亮度
  離散度），查詢端用 75 個滑動視窗，不需要使用者先裁圖。
* `chainway/vision/person.py` —— 人／皮膚偵測，把皮膚與背景排除掉。
* `chainway/vision/keypoints.py` —— ORB 比對。
* `chainway/search/find.py` —— **CLI 與網頁共用的那條流程**（`run()`）。
  改這裡兩邊一起變。
* `chainway/search/fingerprint.py` —— 把影像庫壓成指紋檔（fp2 格式），
  讓比對能離開有圖的那台電腦。含 `verify()` 就地自我驗證。
* `chainway/search/bench.py` —— `cli bench` 自己畫一個庫測比對邏輯；
  `cli bench --roundtrip` 測整條匯出路。都不需要真資料。

## 量過的數字（別再重量一遍）

| 東西 | 結果 |
|---|---|
| 合成庫 150 款 80 題 | Top-1 92.5～95%（但合成衣服紋理單純，關鍵點在那裡偏可靠） |
| 真實庫雜訊底線 | 純亂數圖能拿到 41、59、62、76、93 分 |
| 真的同一件（真照片） | 第一名比第二名高 26～80 倍 |
| 一款存一色 vs 存全色 | Top-1 82.2% → 97.8%（60 款×3 色的合成測試） |
| 顏色數不平均的偏差 | 扣分與不扣分都 98.0%，量不出差別，維持不扣 |
| **真實穿搭照（使用者核對）** | **8 題對 1 題** |

## 使用者已知的正解（目前只有這一筆，很珍貴）

    照片：米白針織上衣、卡其打褶荷葉領片、綁帶蝴蝶結
    正解：KA1269026「壓摺荷葉領綁帶蝴蝶結針織」

當時系統把它排第 2（第 1 名是 KA1165101「荷葉領片長袖襯衫」）。
**正解的九宮格距離是 136.8、主色 ΔE 72.6** —— 顏色完全對不上。
這一筆是判斷任何改動有沒有用的唯一真實依據，先用它驗，再談別的。

## 資料缺口（要使用者補的）

* 1,157 / 3,320 款沒有品名 → 第一關找不到這些款。跑 `cli build` 重建主表。
* 庫存要先跑 `cli ingest`（選單 1），而且 ERP 匯出要含
  「貨品編號＋顏色＋尺寸＋總存」。

## 別重蹈的覆轍

* **合成資料的結論不能當真實庫的結論。** 門檻在 150 款合成庫上訂的，
  搬到 3,320 款真實庫直接失效，這個錯犯過一次。
* **不要拿「取最大值」當融合方式。** 多個候選取 min/max 會讓機會多的
  那一方佔便宜（量過三次都變差）。
* **不要讓後面的關卡推翻前面的關卡。** 證據不夠強就維持原順序。
* **一個錯得很有自信的答案比「我不確定」更糟。** 這是內部工具，同事會
  照著它去翻商品。
