# 進度 —— 以圖查貨號

每查到一個結論就往上加。格式固定：**發現什麼 / 怎麼驗的 / 數字 / 下一步**。
被推翻的結論不刪除，移到最後「已推翻」區並註明原因 —— 看得到死路才不會重走。

---

## 2026-09-16

### 1. 系統圖底部有一條印著貨號的字幕帶，佔畫面 24.3%

**怎麼驗的**：隨機抽 300 張 283×354 系統圖，算「每一列的顏色在不同圖之間差多少」
（跨圖標準差）。字幕帶是後製貼上去的，每張圖都一樣，所以那幾列的跨圖差異會趨近 0。

**數字**
- 全庫 5,298 張系統圖中，**4,082 張（77.0%）是 283×354**
- 字幕帶從 y=268 開始，**佔高度 24.3%**
- 帶內跨圖標準差 **17.28**，帶外 **66.08**；y=290/330/340 幾乎是 **0**（每張圖完全相同）
- 3×3 顏色格的最底排 **73% 是字幕帶**；4×4 是 **97%**

**下一步**：見第 3 條 —— 去掉它的方式很重要。

---

### 2. 品名分品類地不可靠，而且大多數根本沒寫

**怎麼驗的**：用 `master.parquet` 的品名，對照指示書量到的重點尺寸與貨號品類碼。
袖長門檻不用人猜，取「品名寫長袖」與「品名寫短袖」兩群中位數的中點（15.6cm）。

**數字**
- **87.5%（1,005/1,149 款）品名沒寫袖長**。其中量起來偏長 584、偏短 421 ——
  文字對這群完全沒有資訊，尺寸有
- 品名寫「長袖」但量起來偏短：13.7%（7/51）；寫「短袖」但偏長：0%（0/72）
- 品名 vs 貨號品類碼一致率：**襯衫 76.7%**、**裙 83.3%**、**外套 85.9%**，
  但褲 98.5%、針織 99.0%、上衣 99.7%

**結論**：品名不是在說謊，是**缺席**（沒寫）與**分品類地不一致**。
襯衫／裙／外套這三類的文字只能當「一個證人」，不能當判決。

**附帶發現**：`settings.yaml` 目前把袖長當**季別層級**屬性推定，
拿指示書實測驗證，**季別預設有 12–22% 是錯的**
（季別碼 7=79.6%、8=82.0%、5=77.9%、6=88.3%）。指示書對 1,149 款有逐款實測值，
那一群應該直接用量到的。

---

### 3. ★ 字幕帶不要動它 —— 拿掉反而更差（我的修改已回退）

**怎麼驗的**：三組對照，條件**完全相同** —— 同一份程式碼、
`fingerprint.verify` 協定、全庫 3,321 款候選、`simulate()` 退化後的
模擬手機拍當查詢圖、40 題、固定種子。唯一的差別是字幕帶怎麼處理。

**數字**

| 字幕帶處理 | Top-1 | Top-5 |
|---|---|---|
| **原圖（完全不動）** | **15.0%** | 20.0% |
| 填白（保持原尺寸） | **5.0%** | 20.0% |
| 裁切（283×354 → 283×269） | 7.5% | 17.5% |

**結論：拿掉字幕帶沒有幫助，兩種做法都讓 Top-1 變差，填白甚至比裁切更糟。**

原本的推論是「裁切壞在改變長寬比」，所以改成保持尺寸的填白。
**那個解釋被自己的實驗推翻了** —— 填白保持了尺寸，結果更差（5.0%）。
為什麼會這樣目前不知道。

**n=40 很小**：15.0% 是 6/40、5.0% 是 2/40、7.5% 是 3/40，差異未達統計顯著。
但三組裡沒有任何一組贏過「不動」，所以採用「不動」。要翻案先把 n 拉到 ≥300。

**已做的處置**
- `grid.py` / `keypoints.py` / `fingerprint.py` / `selfeval.py` 全部回退，
  比對路徑**不呼叫** `blank_caption_band`
- 函式本身保留在 `imageio.py`（`features/fashion_clip.py` 仍在用，
  CLIP 對大片色塊與文字敏感，那條路上裁掉是合理的），docstring 裡寫明
  三組數字與「不要接進比對流程」
- `data/outputs/指紋` 已還原成基準版，與 9/15 原始備份**逐檔 hash 相同**

**順帶推翻的一組數字**（記錄避免有人再引用）：先前量到「只留衣服 62.5%
vs 原圖 62.5%」，看起來像填白有利。那組的查詢圖是**原圖本身**
（等於拿同一張圖找同一張圖），跟 15.0% 那組的退化查詢圖難度差一個量級，
**兩者不可比**。該組唯一成立的是組內比較：只留字幕帶 Top-1 0.0%
（亂猜 0.03%）→ 字幕帶不帶可檢索訊號。

> ⚠️ **不同條件量出來的數字不能互相比。** 這是這個檔案的規矩。
> 每一個數字都要標明：查詢圖怎麼來的、候選池多大、幾題。
> 這條規矩是使用者指出來的 —— 我先前把 62.5% 跟 15.0% 並列，那是錯的。

---

### 4. 準確率必須指明候選池大小，否則沒有意義

**怎麼驗的**：同一套邏輯（都裁過）在不同候選池跑。

**數字**

| 候選池 | Top-1 |
|---|---|
| 200 款 | 46.7% |
| 400 款 | 33.3% |
| **3,321 款（全庫，真實情境）** | **7.5%** |

候選池翻倍，Top-1 大約掉 13 個百分點。

**含意**：`selfeval` 預設 `pool=200`，但店員實際是在全庫 3,321 款裡找。
**200 款的準確率不能拿來當全庫的準確率。**
80% 這個目標必須綁定「全庫」才有意義。

---

### 5. 現在有 193 題「真實跨拍攝」題目可用，不必人工標註

**怎麼驗的**：盤點所有同一貨號、但來自不同拍攝場合的圖。檔名就是答案。

**數字**
- 同時有「283×354 縮圖」與「高解析實拍」的款：**57**
  （已目視確認 KA0753005：縮圖 vs 5425×3468 吊掛實拍，不同背景、無字幕帶）
- 指示書抽出的**打樣照片**190 款，其中 **136 款**同時有系統圖
- 合計約 **193 題**真實題目，n=193 在 80% 附近的 95% 區間約 ±6pp

**為什麼重要**：合成題（把系統圖退化成手機拍）是**同一張圖**的變形，
真實題是**兩次不同的拍攝**。CLAUDE.md 記錄過「合成資料的結論不能當真實庫的結論」
這個錯已經犯過一次。

**下一步**：把這 193 題做成常設評測指令，當作唯一的計分板。

---

### 6. 資料盤點：桌面上沒有第二個 Raw Data 資料夾

**怎麼驗的**：掃過 `C:\Users\USER\Desktop` 底下所有資料夾。

**數字**
- 只有 `商品設計Raw Data` 有 KA 商品圖（8,248 張）
- `Chainway-data-mining` 的 4,025 張在專案自己的 `data/`，是**產出**不是來源
- 其餘（BCJL參考圖 71、新增資料夾 31、Cindy 4、商品/Scan/AI破解檔 0）都沒有 KA 命名

`商品設計Raw Data` 內部分佈：

| 子資料夾 | KA 圖 | 標準貨號 | settings.yaml |
|---|---|---|---|
| 系統圖 | 4,101 | 4,072 (99%) | ✅ 已設定 |
| 04_企劃 | 2,893 | 2,812 (97%) | ❌ **不該加** |
| 電子目錄 | 1,118 | **4 (0%)** | ❌ 已刻意排除，維持 |
| 裁縫指示書 | 134 | 134 (100%) | ✅ 已設定 |
| 新格紋 | 2 | 0 | ✅ 已設定 |

`04_企劃` 不該加的理由：那 2,893 張全在兩個巢狀專案裡 ——
`商品逆向分析系統_私密手機版` 是 Next.js app，圖在 `dist/client/products/`（打包複製品）；
另一個是 `lineart_previews/`、`plaid_lineage_comparison.jpg`（前一套分析系統的產出）。
加進去等於把線稿與比較圖當成商品照。`settings.yaml:37` 已註明
「00_核心資料 ~ 05_案例 是產出區，不是原始證據來源」。

**結論**：`settings.yaml` 的 `paths` **不需要新增任何路徑**，現有六個路徑全部存在。

---

### 7. 指示書的數值抽取壞了，目前不能拿來做交叉判斷

**怎麼驗的**：看各尺寸欄的分位數是否符合真實服裝。

**數字**
- `chest` 中位 **37.0**（一件上衣的胸圍不可能是 37cm）
- `body_length` 中位 **23.0**，跟 `sleeve_length` 中位 17.0 幾乎一樣
- `sleeve_length` 原始值 min **-421053**、max **960008**
- 各比值變異係數 1.73–1.92（衣長/胸圍 p10 0.158 → p90 1.278，**8 倍散佈**）

**下一步**：先修抽取並驗過，再談用重點尺寸做跨模態判斷。
`imageio.py` 的註解記著上一次踩「量測本身壞掉卻拿去當基準」的代價：Top-1 1.29%。

---

### 8. 穿搭照的比對基準用錯了分母

**發現**：衣服的重點尺寸不會變，但照片上的視覺位置會隨穿著者身高體型而不同
（下擺落在哪裡是「衣服×穿著者」的性質，不是衣服的性質）。

- `vision/locate.py` **做對了** —— 設計重點座標是 `(ys.mean()-y1)/bh`，
  分母是**衣服外框**（191–200 行）
- `vision/grid.py` 的 `photo_signatures` **用錯了** —— 21 個滑動視窗全部以
  `p["外框"]`（**人框**）為比例。同一件衣服穿在不同身高的人身上，
  「人框 30%–70%」切到的部位就不一樣

程式目前用「試 21 段、取最像的」硬扛（註解：「每多一條裁切規則就多一個反例」）。
那是迴避不是解法。

**下一步**：改用衣服自己的外框當分母。比值同時消掉相機距離與穿著者身高兩個未知數。

---

### 9. ★★ 真實基準建立：Top-1 3.6%，而且 Top-5 一模一樣

**怎麼驗的**：建了一個免人工標註的真實跨拍攝評測。
題目來源是系統圖資料夾裡「同一貨號同時有 283×354 縮圖與高解析實拍」的款 ——
兩張是**兩次不同的拍攝**（不同場地、打光、角度），不是同一張圖的變形。
**防洩漏靠結構**：參考指紋只用縮圖重建，大圖完全不進參考庫。

**數字**（候選池 3,316 款）

| | 值 | 95% 區間 |
|---|---|---|
| 題數 | 56 | |
| **Top-1** | **3.6%**（2/56） | [1.0%, 12.1%] |
| **Top-5** | **3.6%**（2/56） | [1.0%, 12.1%] |
| 亂猜 | 0.03% | |

**合成題高估 4 倍**：合成 Top-1 15.0% / Top-5 20.0% vs 真實 3.6% / 3.6%。
先前所有以合成題為基礎的結論都要按這個比例打折。

**Top-1 == Top-5 是最重要的診斷**。答對的兩題都在第 1 名，
答錯的 54 題**正解連前五名都進不去**。這不是排序問題，是表示法問題 ——
指紋裡 96 個數字全部是顏色（2×2 與 4×4 的 LAB ＋ 9 個亮度離散度），
而顏色會被光線改掉。CLAUDE.md 當初說「Top-5 只比 Top-1 高 5 個百分點」
是結構性問題，在真實題目上更極端：兩者完全相等。

> ⚠️ **這 56 題有嚴重的抽樣偏誤，不是全庫的代表性數字。**
> 季號分布：KA078 佔 41 題、KA075 佔 13 題（合計 **96%**），
> 其餘季別只有 2 題。原因是只有這兩季在系統圖裡同時留了縮圖與高解析實拍，
> 其他季別只有縮圖，湊不出跨拍攝配對。
> 所以它量的是「2018 年那批店內實拍參考圖的跨拍攝表現」，
> **不能當成全庫水準引用**。

**目前兩把尺都不完美，各有各的問題：**

| | 代表性 | 真實性 |
|---|---|---|
| 合成題（n=40，全庫抽樣） | ✅ 全庫 | ❌ 同一張圖的變形，高估 4 倍 |
| 真實跨拍攝（n=56） | ❌ 96% 集中在 2018 兩季 | ✅ 兩次不同拍攝 |

改動要兩把尺都看，任何一把單獨都不足以下結論。

---

### 10. 指紋裡沒有任何「圖片特徵」，只有顏色

**怎麼驗的**：讀 `fingerprint._flatten()` 與 `grid.distance()`。

**發現**
- 指紋存的 96 個浮點數：2×2／3×3／4×4 共 29 格的中位 LAB（87 個）
  ＋ 9 格亮度離散度。**全部是顏色。**
- 排序公式只用 `格2 + 格4 + SPREAD_W × 離散`。**`格3` 根本不參與排序**
  （只給人看）。
- `grid.cell_pattern()` **已經算得出**格紋／條紋／素面／局部圖案，
  還有橫能量、縱能量、方向平衡、鋪滿度 —— 但只餵給 `analyse()` 的報表，
  **從來沒有進入比對**。
- `離散` 只回答「這格花不花」，**不帶方向性**，所以格紋（兩方向都規律）、
  條紋（單方向）、繡花（能量集中一小塊）在指紋裡是同一件事。

對一個核心產品線叫「經典格紋」的品牌，這是最大的一塊空白。

**另外**：`find.py` 的「第一關：特徵」是 `if terms:`，而 `terms` 來自
**使用者打的字**。自我驗證傳 `words=""`，店員拍照也不會打字 ——
所以那一關在照片查詢時**完全空轉**，實際只跑「顏色 → 關鍵點」兩關。
`find.py:11` 引用使用者原話「第一是特徵，而不是顏色」，但那個「特徵」
被實作成品名的文字詞，不是圖像特徵。

---

### 11. ★★ garment_mask 在非白底的系統圖上會碎成雜訊

**怎麼驗的**：對 KA0751003（目視確認有白＋粉兩件並排）跑 `garment_mask`，
把遮罩疊回原圖看；再對 250 張隨機縮圖統計外框寬度佔比。

**數字**
- KA0751003 外框 `(0, 34, 256, 207)` —— **寬度佔滿 100%**，橫跨兩件衣服
- 遮罩疊圖看起來是**一片碎斑點**，不是乾淨的衣服區域
- 250 張隨機縮圖：**23% 的外框寬度 ≥85%**（幾乎整寬），53% 落在 45–65%

**為什麼**：`garment_mask` 從整圈邊框取背景色中位數，再用 `BG_TOL` 切。
這批圖有相當比例是**店內／展示間實拍**（灰牆、紋理背景），淺色衣服
（白、淺粉）跟灰牆的距離不夠大，門檻切出來就是斑點。

**影響範圍比切配色更廣**：每一格的顏色中位數都是在這個遮罩上算的。
遮罩壞掉，`cell_signature` 的每一個數字都建立在雜訊上。

### 12. 配色沒有被切開，而且現有的切法對這批圖無效

**怎麼驗的**：對 250 張縮圖跑 `split_garments`，並對 KA0751003 逐欄除錯。

**數字**
- `split_garments` 只存在於 `features/fashion_clip.py`（CLIP 專用路徑），
  比對路徑 `grid.py` / `fingerprint.py` / `find.py` / `refs.py` **從未呼叫**
- 它從**上緣兩角**取背景色，得到純白 `(255,255,255)`，
  但這批圖的實際逐欄中位數跟純白差 **35–121** —— 因為不是白底去背圖
- 結果：tol=14 之下**一欄都不符合**，走道數 0，永遠切不開
- 放寬到 tol=40 並填白字幕帶後，x=105–120 出現 15px 走道 ——
  **縫是在的，是背景假設錯了**

`settings.yaml:228` 自己寫著「不切的話…相似度被稀釋，這是以圖搜貨號
不準**最大的單一原因**」。解法寫了，但寫在用不到的那條路上，而且假設錯誤。

**這是同一個模式的第三次出現**：`simulate()` 假設棚拍平放圖、
`strip_caption_band` 寫在 CLIP 路徑、`split_garments` 假設白底 ——
**程式對這批資料長什麼樣的假設是錯的**，而不是演算法不夠好。

---

### 13. ★★★ 答案是換表示法，不是調參數：CLIP 比純 numpy 準 22 倍、快 18 倍

**怎麼驗的**：建了一個**來源獨立**的評測。查詢圖是裁縫指示書 `.xls` 裡抽出來的
打樣照片（衣服掛在衣架上、素色牆、手機拍），參考庫是系統圖 —— 兩套完全不同
的攝影，不是同一次拍攝。非商品照做成接觸印樣**逐張看圖挑掉** 33 張
（電商截圖、T恤線稿、燙鑽尺寸圖、印花稿、色票表、壞檔），留下 173 張，
其中 122 題的貨號在參考庫裡。四種方法跑**同一批題目、同樣全庫候選**。

**數字**（候選池 3,320 款，亂猜 Top-1 0.03%）

| 方法 | Top-1 | Top-5 | 速度 |
|---|---|---|---|
| A 現行（顏色→關鍵點重排） | 2.5% (3/122) | 6.6% | 6.5 秒/題 |
| B 關鍵點全庫檢索 | 1.6% (2/122) | 8.2% | 2.0 秒/題 |
| C 裸 CLIP（無 centering） | 47.5% (58/122) | 69.7% | |
| D VisualIndex（含 centering） | **50.0%** (61/122) | **71.3%** | |
| **E ＋ 限定品類** | **55.7%** (68/122) | **76.2%** | **0.36 秒/題** |

D 的 95% 區間 [41.3, 58.7]，E 是 [46.9, 64.2]。

**結論：瓶頸是表示法，不是參數。** 純 numpy 的方法在比「這格是什麼顏色」
「這裡有沒有邊緣」；CLIP 比的是「這是一件什麼樣的衣服」。今天試過的每一個
numpy 層級的改動（字幕帶、遮罩、切配色、關鍵點檢索）都沒有動搖 2% 這個數字。

**而且那些想法 CLIP 路徑早就做了**：`settings.yaml` 的 `strip_caption: true`、
`split_colorways: true` 都只接在 CLIP 上。我花了一整天重新發現並實作到
numpy 路徑，結果證明對那條路沒用。

**兩個既有但沒被當回事的功能**：`centering`（扣掉整批平均向量，解決 CLIP
「什麼都很像什麼」）值 +2.5pp；`search_by_image(category=...)` 限定品類值 +5.7pp。

**離線可行**：torch 2.13.0+cpu 與 transformers 5.16.1 已裝在這台機器上，
參考向量算一次存成 9MB 檔案，查詢時只對一張圖算向量 —— 不連外、不呼叫 API。
使用者定的「不准用模型」是為了做成靜態內部工具，這個形式仍然符合那個目的。

---

## 已推翻（不要再走一次）

### ✗ 「字幕帶洩漏答案，ORB 在讀印上去的貨號」

**當時的理由**：ORB 關鍵點 34.0% 落在只佔 24% 面積的字幕帶內
（逐張中位 35.4%，p75 45.5%，最高 82.8%），看起來明顯偏好那裡。

**怎麼推翻的**：把衣服整個塗白、只留字幕帶去查 —— **Top-1 0.0%、Top-5 0.0%**，
連亂猜（0.03%）都不如。字幕帶不帶任何可檢索的訊號。

關鍵點是會落在字形上，但那些關鍵點**配不出正確的款**。

### ✗ 「品名寫長袖但量起來很短的有 50/51 款」

門檻（35cm）是我自己猜的，而 `sleeve_length` 不是整條手臂的長度
（長袖中位 23.0cm、短袖 8.2cm，量法不同）。用資料自己定的門檻重判是 **13.7%**。

### ✗ 「同款內 std 0.000、跨款 std 0.391，比值穩定 39 萬倍」

同一貨號的多列是**同一份文件重複抽取**，值完全相同，所以同款內 std 必然是 0。
那不是「比值穩定」的證據。

### ✗ 「指示書有 1,244 張打樣照片」

讀到的是重判前的 `kind_guess`。正確的 `kind` 欄是 **206 張 / 190 款**。

---

## 下一步（依序）

1. ~~字幕帶改填白~~ **已做，已推翻，已回退**（見第 3 條）
2. 把 193 題真實跨拍攝題目做成常設評測指令，之後所有數字都以它為準。
   **這是目前最該做的一件事** —— 現在全部的結論都建立在 n=40 的合成題上，
   40 題的解析度是 2.5 個百分點一題，根本量不出 5pp 以內的差異。
   沒有這個儀器，後面每一次改動都會像這次一樣繞一大圈才發現沒用。
3. `photo_signatures` 改用衣服外框當分母（現在用人框，會隨穿著者身高變動）
4. 修指示書數值抽取，補一道合理範圍過濾
5. 品名缺口 1,157 款：需要涵蓋舊季別的 POS Excel，只有使用者拿得到

## 給下一個接手的人

目前全庫（3,321 款）的實際水準是 **Top-1 15.0% / Top-5 20.0%**，
目標 80% 還差很遠。而且要注意 Top-5 只比 Top-1 高 5 個百分點 ——
正常檢索系統 Top-5 會遠高於 Top-1（例如 85%/97%）。
兩個數字貼這麼近，代表答錯的時候正解**根本不在前面**，
不是差一點。這是結構性問題，不是調參數能救的。CLAUDE.md 一開始就指出
這一點，到現在仍然成立 —— 字幕帶不是那個結構性原因。
